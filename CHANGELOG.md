## Next

Features:
- **BREAKING**: `async` subscribers are now supported. The `EventDispatcher.dispatch` returns a future. 
- **BREAKING**: The `@Subscribe` annotation supports now a priority. This allows you to run the subscribers in a specific order (low to high).

## 1.0.0

- Initial version.
